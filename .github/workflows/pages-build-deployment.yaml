name: pages-build-deployment

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-deploy:
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3
